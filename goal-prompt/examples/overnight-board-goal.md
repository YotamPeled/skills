/goal Make docs/REENTRY-SPEC.md section 8 fully true on the live board, verified by running: uv run pytest -q; python tests/red_when_removed.py; the board API at http://127.0.0.1:8931/api/board?project=boxes; the page at http://127.0.0.1:8931/?project=boxes read cold, top to bottom; a cold review by a separate session given only that page and spec section 1, written to docs/COLD-REVIEW.md; and docker compose up -d --build with the container's files matching the tree. If a check cannot run, record why in docs/LOOP-STATE.md, treat the item it covers as unverified rather than done, and keep working on the items that do not depend on it. Use only <repo>, the board box boxes/F1 (attach to it with your own id as spec section 9 says, and work through the board: chain, claims, checkpoints, evidence, findings), and spec sections 1-9; section 10 is settled and stays closed. While preserving: never edit the database by hand; never kill, close or restart another agent's window or process; no new spend; never weaken or delete a check; nothing marked done that was not demonstrated by running it. Between iterations, read docs/LOOP-STATE.md first (create it on the first pass: the six section-8 items with a boolean each, an attempt ledger kept and reverted alike, rulings with their cost if wrong, and noticed-but-not-touched), take the section-8 items in order, record what changed and what the evidence showed, commit small; an item that fails three times gets a ledger entry and a finding on the box, then move on to the next independent item, which is progress and not a blocked goal. If you hit a blocker with no valid path left, report the paths tried, the evidence, the blocker and the input needed, as a yellow box with its origin, keep working on what does not depend on the owner, and leave the goal active; only when that same blocker has stopped you on three consecutive goal turns is the goal blocked. Mark the goal complete only when every section-8 item is demonstrated live, boxes/F1 is finished with evidence, and the boxes project's brief is written as the very last thing.
